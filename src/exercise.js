{
  const createForm = () => {
    // CREATE a form element

    // CREATE an HTML imput element for First Name

    // ADD input element to form

    // CREATE an HTML imput element for Last Name

    // ADD input element to form

    // CREATE an HTML button element

    // ADD button element to form

    // FIND main element

    // ADD form to main element
  };

  const formSubmittedListener = () => {
    
  };

  const main = () => {
    createForm();
    formSubmittedListener();
  };

  window.onload = () => main();
}